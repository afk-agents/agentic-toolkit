# Claude Code Experiments

This repository contains custom extensions for Claude Code:

- `commands/` - Custom slash commands
- `skills/` - Custom skills
- `agents/` - Custom subagents
- `mcp-servers/` - MCP server implementations
